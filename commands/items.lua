-- items.lua — mv, cp, rm, chmod (stubs)

local function mv(_, _)
    return "mv: not yet implemented."
end

local function cp(_, _)
    return "cp: not yet implemented."
end

local function rm(_, _)
    return "rm: not yet implemented."
end

local function chmod(_, _)
    return "chmod: not yet implemented."
end

return { mv = mv, cp = cp, rm = rm, chmod = chmod }
